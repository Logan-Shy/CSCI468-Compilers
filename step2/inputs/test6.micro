PROGRAM test
BEGIN
	INT a,b,c,x,y,z,h,j,k;
	FUNCTION VOID printout (INT a,INT b,INT c)
	BEGIN
		WRITE (a);
		WRITE (b);
		WRITE (c);
	END
  FUNCTION INT main()
	BEGIN
	IF (a = b)
		
		h:=0;
	ELSE

	     WHILE(j <= 10)
		     a := j;
		     j := j + 1;		
	     ENDWHILE

	ENDIF
	
	RETURN a+b;
	END
END
